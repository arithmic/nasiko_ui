name: nasiko_ui_gallery
description: "Component gallery for the Nasiko UI design system."
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  nasiko_ui:
    path: ../

flutter:
  uses-material-design: true
